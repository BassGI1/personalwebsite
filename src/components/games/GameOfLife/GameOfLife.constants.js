const GameOfLifeConstants = {
	NUM_ROWS: Math.floor((16 * 20) / 7),
	NUM_COLS: Math.floor((16 * 13) / 7),
	RANDOM_LIFE_PROBABILITY: 0.2,
	GAMELOOP_DURATION: 333,
}

export default GameOfLifeConstants
